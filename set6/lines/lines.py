import sys

EXTEN = [".md", ".py", ".txt"]

if len(sys.argv) > 1:
    for i in  sys.argv:
         if i == ".md":
             print("vamos")






# Hacer una array para recorrer la cantidad de extensiones que permite nuestro programa y validar
# Hacer una funcion donde valide que el archivo existe y haga la validacion si el mismo esta o no dando un mensaje de error
# Hacer que cuando este adentro del programa lea todo el archivo y nos diga cuantas lineas de codigo tiene sin contar espacios y lineas
